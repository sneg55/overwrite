# Multi-package build: templates package + test package.
# `overwrite-vault` (templates, deployable) must build before `overwrite-test`
# (Daml Script), which data-depends on the templates DAR.
packages:
  - .
  - ./test
