# Copy to .env and fill in. The .env file is gitignored and is read only by
# `npm run tts` / `npm run build` via Node's --env-file. Never commit it.
OPENAI_API_KEY=sk-...
# Optional overrides:
# TTS_VOICE=sage
