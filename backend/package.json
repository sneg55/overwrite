{
  "name": "@overwrite/backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Overwrite backend: JSON Ledger API client, epoch scheduler, oracle poller, MM simulator, REST API",
  "scripts": {
    "dev": "bun run --watch src/entrypoints/server.ts",
    "start": "bun run src/entrypoints/server.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.0",
    "bun-types": "^1.3.14"
  }
}
